# Çalışma zamanı bağımlılıkları (python main.py için gereken tek set).
# Kurulum: pip install -r requirements.txt
# Test edildiği sürümler: Python 3.11.2 / pandas 2.3.2 / openpyxl 3.1.5
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0
